<a
  class="brand"
  href="https://github.com/GeorgesAlkhouri/sealbreak"
  aria-label="Sealbreak on GitHub"
>
  <svg
    class="brand__mark"
    viewBox="0 0 49 58"
    role="img"
    aria-hidden="true"
  >
    <g filter="url(#brand-shadow)">
      <path
        d="M24.25 4L40.5 9.625V20.875C40.5 32.125 34.25 39.625 24.25 45.25C14.25 39.625 8 32.125 8 20.875V9.625L24.25 4ZM24.25 9.625L13 13.375V20.875C13 28.375 16.75 34 24.25 39C31.75 34 35.5 28.375 35.5 20.875V13.375L24.25 9.625Z"
        fill="var(--cream)"
      />
    </g>
    <defs>
      <filter id="brand-shadow" x="-8" y="-4" width="65" height="67">
        <feDropShadow
          dx="0"
          dy="4"
          stdDeviation="4"
          flood-color="#000000"
          flood-opacity="0.35"
        />
      </filter>
    </defs>
  </svg>

  <span class="brand__copy">
    <span class="brand__name">Sealbreak</span>
    <span class="brand__tagline">SECURE ACCESS</span>
  </span>
</a>

<style>
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    color: inherit;
    text-decoration: none;
  }

  .brand__mark {
    width: 2.75rem;
    height: auto;
    flex: none;
  }

  .brand__copy {
    display: grid;
    gap: 0.05rem;
  }

  .brand__name {
    color: var(--cream);
    font-size: clamp(1.55rem, 1.35rem + 0.6vw, 2rem);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -0.02em;
    text-shadow: 0 4px 4px rgb(0 0 0 / 0.22);
  }

  .brand__tagline {
    color: var(--secondary-text);
    font-size: 0.7rem;
    font-weight: 650;
    letter-spacing: 0.2em;
  }

  .brand:focus-visible {
    border-radius: 0.5rem;
    outline: 3px solid var(--sun);
    outline-offset: 0.4rem;
  }
</style>
